version: '2'
services:
  frontend-app:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      - VIRTUAL_HOST=admin.ibisubizo.com
      - LETSENCRYPT_HOST=admin.ibisubizo.com